{%extends 'app:layout'%}

{%section hello%}
    {% with user %}
        {% if !empty(.last) %}
            <h1>Hello, {{ adf().login}} {{ .last ~ .first }}</h1>
            {% for image in .images() %}
                <img src="{{ image.path }}" />
            {% forelse %}
                <h1>Нет изображений</h1>
            {% endfor %}
        {%endif%}
    {% endwith %}
{%endsection%}
