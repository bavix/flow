closed: true

properties:
  args:
    types:
      - any

syntax:
  - '\({{ args }}\)'
  - '{{ args }}'

directives:

  elseif:
    properties:
      args:
        types:
          - any

    syntax:
      - '\({{ args }}\)'
      - '{{ args }}'

  else:
