closed: true

properties:
  args:
    types:
      - any

syntax:
  - '{{ args }}'

directives:

  elseif:
    properties:
      args:
        types:
          - any

    syntax:
      - '{{ args }}'

  else:
