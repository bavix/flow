closed: true

properties:
  rows:
    types:
      - variable
      - callable
      - array
      - ternary
      - range

syntax:
  - '{{ row }} in {{ rows }}'
  - '{{ rows }} as {{ row }}'
  - '{{ rows }} as {{ key }} => {{ row }}'
